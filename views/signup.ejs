<!DOCTYPE html>
<html lang="en">

<head>
    <title>Sign Up page</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <style>
        /* Custom CSS for animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .animate-fadeIn {
            animation: fadeIn 0.5s ease-out;
        }
        body{
            background-image: url("../public/beautiful-office-space-cartoon-style.jpg");
            background-size: cover; /* Cover the entire background */
            background-repeat: no-repeat; /* Prevent background image from repeating */
            background-position: center;
        }
    </style>
</head>

<body>
    <div class="container flex justify-center items-center h-screen">
        <div class="bg-white p-8 rounded-lg shadow-md min-w-[320px] max-w-md w-full animate-fadeIn">
            <h6 class="text-2xl font-semibold mb-4 text-center text-gray-800">Signup as a New User</h6>

            <% if (errors && errors.length > 0) { %>
            <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4" role="alert">
                <p class="font-bold">Oops! Something went wrong:</p>
                <ul>
                    <% errors.forEach(error => { %>
                    <li><%= error.message %></li>
                    <% }); %>
                </ul>
            </div>
            <% } %>

            <form action="/signup" method="post" class="space-y-4">
                <div>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <label for="firstName" class="block text-sm font-semibold text-gray-700">First Name:</label>
                    <input type="text" name="firstName" class="border border-gray-400 rounded-md p-2 w-full focus:outline-none focus:border-green-500 transition duration-300">
                </div>
                <div>
                    <label for="lastName" class="block text-sm font-semibold text-gray-700">Last Name:</label>
                    <input type="text" name="lastName" class="border border-gray-400 rounded-md p-2 w-full focus:outline-none focus:border-green-500 transition duration-300">
                </div>
                <div>
                    <label for="email" class="block text-sm font-semibold text-gray-700">Email:</label>
                    <input type="email" name="email" class="border border-gray-400 rounded-md p-2 w-full focus:outline-none focus:border-green-500 transition duration-300">
                </div>
                <div>
                    <label for="password" class="block text-sm font-semibold text-gray-700">Password:</label>
                    <input type="password" name="password" class="border border-gray-400 rounded-md p-2 w-full focus:outline-none focus:border-green-500 transition duration-300">
                </div>
                <!-- <div class="flex items-center">
                    <input type="checkbox" name="isAdmin" class="mr-2">
                    <label for="isAdmin" class="text-sm text-gray-700">Admin</label>
                </div> -->
                <div>
                    <button type="submit" class="bg-green-500 text-white font-semibold py-2 px-4 rounded-md hover:bg-green-600 transition duration-300">Signup</button>
                </div>
                <p class="text-sm mt-2 text-center text-gray-700">Already have an account? <a href="/login" class="text-green-600 font-semibold">Login</a></p>
            </form>
        </div>
    </div>
</body>

</html>
