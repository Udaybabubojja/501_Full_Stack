<!DOCTYPE html>
<html lang="en">

<head>
    <title>TO-DO Manager</title>
    <link rel="stylesheet" href="./css/styles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <h6 class="py-4">Signup as a new User</h6>
            
            <% if (errors && errors.length > 0) { %>
                <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4" role="alert">
                    <p class="font-bold">Oops! Something went wrong:</p>
                    <ul>
                        <% errors.forEach(error => { %>
                            <li><%= error.message %></li>
                        <% }); %>
                    </ul>
                </div>
            <% } %>

            <form action="/signup" method="post">
                <div class="py-3">
                    <label for="firstName">First Name: </label>
                    <input type="text" name="firstName" class="border border-gray-600 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="lastName">Last Name: </label>
                    <input type="text" name="lastName" class="border border-gray-600 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="email">Email: </label>
                    <input type="email" name="email" class="border border-gray-600 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="password">Password: </label>
                    <input type="password" name="password" class="border border-gray-600 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="isAdmin">Admin: </label>
                    <input type="checkbox" name="isAdmin" class="mr-2">Yes
                </div>
                <div class="py-3">
                    <button type="submit" class="text-white bg-green-500 hover:bg-green-900 font-medium rounded px-5 py-2"> Signup</button>
                </div>
                <p class="py-2">Already have an account? <a href="/login" class="text-green-600">Login</a></p>
            </form>

        </div>
    </div>
</body>

</html>
